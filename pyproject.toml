[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gads-manager-monitor"
version = "0.0.0"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["core"]
